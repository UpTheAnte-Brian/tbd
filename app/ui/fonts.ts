import { Inter, Lusitana, Domine } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const lusitana = Lusitana({
  weight: ["400", "700"],
  subsets: ["latin"],
});

// body text
export const domine = Domine({
  weight: ["400", "700"],
  subsets: ["latin"],
});
