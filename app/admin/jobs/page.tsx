import AdminPlaceholder from "@/app/admin/_components/AdminPlaceholder";

export const dynamic = "force-dynamic";

export default function AdminJobsPage() {
  return (
    <AdminPlaceholder
      title="Admin · Jobs"
      description="Runbook, last run summaries, and failure alerts."
    />
  );
}
